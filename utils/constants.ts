export enum ROOT_ROUTES {
    AUTH = 'auth',
    TABS = 'tabs',
}

export enum AUTH_ROUTES {
    LOGIN = 'login',
    REGISTER = 'register',
}

export enum TAB_ROUTES {
    HOME = 'home',
    PRODUCTS = 'products',
}
