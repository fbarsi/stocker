export enum ROOT_ROUTES {
    AUTH = 'auth',
    TABS = 'tabs',
}

export enum AUTH_ROUTES {
    LOGIN = 'login',
    REGISTER = 'register',
}

export enum TAB_ROUTES {
    HOME = 'home',
    PRODUCTS = 'products',
}

export enum STACK_ROUTES {
    NAV_STACK_TABS = 'nav_stack_tabs',
    NEW_PRODUCT = 'new-product',
    NEW_BRANCH = 'new-branch',
    CREATE_COMPANY = 'create-company',
    ITEMS_CATALOG = 'items-catalog',
    NEW_ITEM = 'new-item',
}

